{{-- <div x-data="{ open: @entangle('show') }" x-show="open" class="fixed bottom-5 right-5 bg-gray-800 text-white p-4 rounded-lg shadow-lg">
    <div class="font-bold text-lg">{{ $title }}</div>
    <div>{{ $body }}</div>
</div> --}}

{{-- <div>{{$employerId}}</div> --}}

<div>
    <div class="font-bold text-lg">{{ $title }}</div>
    <div>{{ $body }}</div>
</div>
